export enum WalletTypes {
    All = 'all',
    Aptos = 'aptos',
    Sui = 'sui',
    Evm = 'evm',
}

export enum FetchStatus {
    FETCHING = 'FETCHING',
    FETCHED = 'FETCHED',
    ERROR = 'ERROR',
    INITIAL = 'INITIAL',
}
